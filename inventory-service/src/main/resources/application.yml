server:
  port: 8083
spring:
  config:
    import: optional:eureka-server:http://localhost:8761
  application:
    name: inventory-service
management:
  endpoints:
    web:
      exposure:
        include: ['*']
