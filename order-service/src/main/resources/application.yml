server:
  port: 8082
spring:
  config:
    import: optional:eureka-server:http://localhost:8761
  application:
    name: order-service
management:
  endpoints:
    web:
      exposure:
        include: ['*']
